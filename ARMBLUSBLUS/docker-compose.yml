version: '3'
services:
  chall:
    build: .
    ports:
      - 10012:10012
    read_only: true
    cap_drop:
      - all
              
    restart: on-failure

    deploy:
        resources:
            limits:
              cpus: '0.2'
              memory: 1000M